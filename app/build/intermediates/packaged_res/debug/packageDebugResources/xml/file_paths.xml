<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Private app files — where final contact photos are stored -->
    <files-path
        name="contact_photos"
        path="contact_photos/" />
    <!-- Cache root — where the camera temp file is written -->
    <cache-path
        name="camera_capture"
        path="." />
</paths>
